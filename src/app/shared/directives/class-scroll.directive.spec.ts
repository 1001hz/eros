import { ClassScrollDirective } from './class-scroll.directive';

describe('ClassScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassScrollDirective();
    expect(directive).toBeTruthy();
  });
});
